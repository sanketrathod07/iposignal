MONGO_URI=mongodb+srv://<db_username>:QHppEKvCcuichy6l@cluster0.ah7v3gg.mongodb.net/?appName=Cluster0
JWT_SECRET=change_this_to_a_long_random_string
PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# Phase 2 — optional. Leave blank to skip YouTube collection (Reddit + News work with no key).
YOUTUBE_API_KEY=AIzaSyB75Iay5UTlhKjy7v8zZJLeqoPYSB7wPR0

# Phase 2 — optional. Set RUN_CRON=true to run the daily ranking-snapshot +
# social-collection job in-process on a schedule (cron syntax), instead of
# an external cron entry calling `npm run daily-job`.
RUN_CRON=true
CRON_SCHEDULE=0 7 * * *
